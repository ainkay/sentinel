# sentinel
SENTINEL IDS is an ML-powered Intrusion Detection System built with Python and Streamlit. Using the Isolation Forest algorithm, it detects zero-day network traffic anomalies without labeled data. This MVP features a real-time threat dashboard and PCA analytics, though its backend architecture requires significant refactoring prior to production.
